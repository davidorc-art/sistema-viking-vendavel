import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.davidorc.sistema',
  appName: 'Sistema Profissional de Gerenciamento',
  webDir: 'dist'
};

export default config;
